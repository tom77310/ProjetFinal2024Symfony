{% extends 'base.html.twig' %}
{% block body %}
   <link rel="stylesheet" href="{{ asset('CSS/FormModifInfos.css') }}">
    <title>Inscription</title>

    <!-- Body -->
        <h1>Formulaire d'inscription</h1>
    <div class="FormulaireModifInfos">
            {{form(form)}}
    </div>

{% endblock %}